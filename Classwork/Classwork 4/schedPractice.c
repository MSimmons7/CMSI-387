#include <stdio.h>

   int main( int argc, char * argv[] ) {
      
      char *names[MAX];
      int *times[MAX];
      fgets(names, MAX, stdin);
      fgets(times, MAX, stdin);
      for (int i = 0; i < times)

   }
